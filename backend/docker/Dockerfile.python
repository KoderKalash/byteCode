# Pinned: "latest" silently changes the language version under your users.
FROM python:3.11-slim

# No COPY of the project. The submission is bind-mounted into /sandbox at run
# time by the executor, so this image holds only the toolchain.
WORKDIR /sandbox

# The command is supplied per-invocation by executor/language/python.js.
